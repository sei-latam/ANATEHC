
var.listSEI <- function(){
  extent.name <- c()
  short.name <- c("BS", "SD", "ST", "QL", "HR", "VD-1", "VD-2", "VD-V", "NV", "EV", "TS", "VD", "PT")
  units.var <- c()
  DF.var <- data.frame(var.name = , short.name = short.name, unit = units.var)
  return(DF.var)    
}

